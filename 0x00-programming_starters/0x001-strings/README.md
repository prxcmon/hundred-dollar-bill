## Strings
"Strings" are a set of words, much like a sentence. I.e. "Hello, world!" While array, especially in C, is a list of _n_ elements of a specific data type.
- This means a 20-character array is simply 20 adjacent characters located in memory.

Declaring strings with arrays can be done within two ways:
- `char_array.c`: Defining each characters in each array, one-by-one
- `char_array2.c`: